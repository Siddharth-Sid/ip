package duke.command;

/**
 * Enum to encapsulate the type of Commands
 */
public enum CommandTypes {
        ADDCOMMAND,
        BYE,
        COMMAND,
        DEADLINE,
        DELETE,
        DONE,
        EMPTY,
        EVENT,
        FIND,
        LIST,
        TODO,
        UNKNOWN
}